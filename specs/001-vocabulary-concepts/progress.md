# Progress: 001-vocabulary-concepts

Append-only run log. Newest last.

- 2026-07-23 — S0 INTAKE: grilled to shared understanding; issue #15 accepted, labelled `feature`.
- 2026-07-23 — S1 SPECIFY: spec.md (4 stories, FR-001–008, SC-001–005), decisions.md, requirements checklist. No unresolved clarifications.
- 2026-07-23 — S2 SETUP: epic #15 promoted (FS-001); stories #20–#23; branch pushed; draft PR #24; title lint green.
- 2026-07-23 — GATE_SPEC: approved by Sam.
- 2026-07-23 — S3 PLAN: plan.md, research.md (R1–R8), data-model.md, contracts/python-api.md, quickstart.md, tasks.md (T001–T013). Constitution Check passed (Article IX lifecycle bits deferred to #19 by ruling). Analyze: green, no CRITICAL. feature-state.json initialised (all tasks `todo`). Awaiting Plan gate.
- 2026-07-23 18:15 — US1 T001–T005 (Implementer, subagent). **Did**: T001 added `CONTROLLED_VOCABULARIES_BASE_URI` to `tests/settings.py`; T002 added `controlled_vocabularies/conf.py` (`get_base_uri()`, `DEFAULT_BASE_URI = "http://localhost:8000/vocabularies"`); T003 wrote failing `tests/test_scheme.py` (11 cases: slug-from-name, rename→slug, empty+whitespace name→ValidationError, non-Latin "Wärmefluss"→"wärmefluss", collision refused, uri composition/rename, `__str__`, delete); T004 implemented `ConceptScheme` (slug via `slugify(allow_unicode=True)`, empty→ValidationError, collision→ValidationError, `uri` property); T005 generated `0001_initial`. **Verified**: baseline `poetry run pytest` = 2 passed (green before start); T003 failed correctly with `ModuleNotFoundError: controlled_vocabularies.models`; after T004 `poetry run pytest tests/test_scheme.py` = 11 passed; full gate `poetry run pytest` = 13 passed, `ruff check .`/`ruff format --check .`/`mypy` all clean, `makemigrations --check --dry-run` = "No changes detected". **Next**: US2 (T006–T008) — `Concept` model + `test_concept.py` + `0002` migration.
- 2026-07-23 — US2 T006–T008 (Implementer). `Concept` (scheme FK cascade, label, slug unique-in-scheme via pre-save check + UniqueConstraint backstop, `uri`); `test_concept.py` (13 cases) RED→GREEN; `0002_concept`. Verified: 26 passed; ruff/format/mypy/migration-check/deptry green. Report on #21.
- 2026-07-23 — US4 T011 (Implementer). Test factories (`ConceptSchemeFactory`, `ConceptFactory`); `test_factories.py` (6 cases) RED→GREEN; `factory_boy` from `mvp-shared[test]`, no dep added. Verified: 32 passed; all gates green. Report on #23.
- 2026-07-23 — US3 T009–T010 (Implementer). `Concept.objects.get_by_uri()` resolver; `test_identity.py` (7 cases) RED→GREEN; no migration. Verified: 33 passed; all gates green. Report on #22.
- 2026-07-23 18:27 — S5 CONVERGE: 13/13 tasks; full verify green; tamper-check flag on `tests/settings.py` triaged (settings module, not a test — decisions.md item 8). Enter S6.
- 2026-07-23 18:31 — S6 REVIEW: 2 low-severity findings, both fixed (`0469ab4`) — `get_by_uri` now requires input under the base; added a first-class FR-006 vocabulary+slug retrieval test. 41 tests green; no CRITICAL/HIGH. Full review posted on PR #24. Enter merge gate.
- 2026-07-23 (backfill) — US2–US4 reports posted to their story issues and the entries above reconstructed from the worker sessions + git history: these steps ran before the report-to-issue rule and were not logged live. Also landed: `codecov.yml` (project 90% / patch 85%) and the matching constitution coverage-bar reconciliation.
- 2026-07-23 20:30 — SPEC AMENDMENT (refine ritual, first use): reverted ad-hoc standards commit a92e812 (process breach — code changed at merge gate without spec/tasks/tests). refine.update added US-5 + FR-009–011 + SC-006 to spec.md; propagated to plan.md (Constitution XII/XIII rows), tasks.md (Phase 8, T014–T016), data-model.md. Story issue #25 created + linked; epic story list updated; title lint green. Delta approved by Sam in-session. State GATE_MERGE → IMPLEMENT.
- 2026-07-23 22:36 — US5 T014–T016 (Implementer, subagent). **Did**: T014 wrote failing `tests/test_standards.py` (11 cases) — walks concrete/editable/non-auto `_meta` fields on both models asserting lazy `verbose_name` + non-empty lazy `help_text`; asserts `Meta.verbose_name`/`_plural` lazy; asserts the four user-facing `ValidationError` messages are lazy `Promise`s with named `%(slug)s` placeholders (collisions) via `error_dict[field][0]`; guards `slug.unique`, `scheme.db_index`, `(scheme, slug)` `UniqueConstraint`. T015 `models.py`: `gettext_lazy as _`; `verbose_name` + `help_text` on all six fields; `Meta.verbose_name`/`_plural` (vocabulary/vocabularies, concept/concepts); collision messages use nested `ValidationError(_(msgid), params={"slug": …})` to keep the msgid lazy (lazy `%` collapses to str — decisions.md item 9); empty-name/label use bare `_()`; `DoesNotExist` left unwrapped. T016 generated metadata-only `0003_alter_concept_options_…` (AlterModelOptions + AlterField, no schema/index/constraint change). **Verified**: baseline `poetry run pytest` = 41 passed (green before start); T014 RED = 8 failed, 3 passed (indexing guards already held) — failures on absent help_text/verbose_name/lazy messages, the right reason; after T015 `poetry run pytest tests/test_standards.py` = 11 passed, full `poetry run pytest` = 52 passed; `makemigrations --check --dry-run` = "No changes detected"; `migrate` applies 0003 OK; `ruff check .` = All checks passed, `ruff format --check .` = 14 files already formatted, `mypy` = Success (4 files), `deptry .` = Success. Commits `2b4d026` (T014), `efa29c3` (T015), `61103e0` (T016). **Next**: Phase 8 complete — re-run final verify (T013) at converge; ship base `en` catalog under `locale/` + `makemessages`-clean CI gate is the Article XII follow-up (out of this story's scope).
- 2026-07-23 23:05 — US5 CONVERGE: parent re-verification — RED-first proven independently (checkout `2b4d026`: 8 failed/3 passed, metadata/i18n assertions the cause); all six gates re-run green by parent (52 passed, ruff/format/mypy/migration-check/deptry); tests read (the `_meta` walk holds future fields to the standard automatically; collision tests pin lazy `Promise` + named placeholder via `params`); migration confirmed metadata-only. Merged `001-vocab-us5` --no-ff; worktree cleaned; plain-language report on #25. Reviewer dispatched over the amendment diff.
