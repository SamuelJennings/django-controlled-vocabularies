# Progress — FS-016 Narrow a field's choices to part of a vocabulary

| When (Europe/Berlin) | Event |
|---|---|
| 2026-08-25 | S0 INTAKE — grilled from free text; hierarchy axis added to scope, targets named by slug, branch is inclusive of its root, ordered-collection sequence honoured conditionally. Issue #164 filed (`feature-request` + `accepted`, milestone v0.1.0). |
| 2026-08-25 | S1 SPECIFY — `specs/016-narrow-field-choices/` created on branch `016-narrow-field-choices`. spec.md: 7 stories, 14 FRs, 7 SCs. Coverage scan resolved 3 further ambiguities into `## Clarifications`; 7 decisions recorded in decisions.md. Spec lint green. |
| 2026-08-25 | S2 SETUP — #164 promoted to epic; stories #165–#171 created and linked; draft PR #172 opened (bot-authored, milestone v0.1.0, `Closes` block for epic + all 7 stories). Title lint green. |
| 2026-08-25 | **Spec gate: APPROVED** by Sam. Proceeding to S3 PLAN. |
| 2026-08-25 | S3 PLAN — research.md R1–R8, plan.md, tasks.md T001–T025, ledger. Machine checks green. |
| 2026-08-25 | S3R DESIGN REVIEW — one reviewer, three lenses, single round. `request_changes`: 1 high, 4 medium, 1 low. All six applied. The high: the ordered sequence was placed on the widget queryset, which the selection control never renders — moved to `ConceptAutocompleteView.apply_ordering()`, empty-query only (D8). Also: test-app models and their migration added to plan scope; W005 resolves on (vocabulary, target) pairs; branch closure cost recorded as per-request and unauthenticated (D9); cycle construction and the relation-model prose corrected. |
| 2026-08-25 | **Plan filed for veto**, not gated. Proceeding to S4 IMPLEMENT. |
| 2026-08-25 | US-1 T006-T012 (issue #165) — `_resolve_restriction` gained the collection subquery term (T006); a member of a `collection`-restricted `ConceptField` validates and a non-member is refused (T007); the refusal names the collection via a new `invalid_restricted` message, one static msgid with the restriction as a single named placeholder (T008); the ModelForm and this package's own widget both offer exactly the collection's members, by count and membership (T009); membership changes reach the field with no restart (T010); `tests/testapp` gained `CoreSample`/`DrillCore` and one consolidated migration, `makemigrations --check` clean across all apps (T011); `_refuse_concepts_outside_vocabulary` renamed to `_refuse_concepts_the_restriction_does_not_admit`, rebound with the field rather than `vocabulary`, and re-expressed against the resolved restriction on both its forward and reverse branches (T012). One pre-existing T005 test's assertion was updated rather than left to fail — decisions.md D10. Full suite: 1752 passed, 1 skipped (was 1729/1, +23 new). `ruff`/`mypy`/`deptry` via pre-commit all green; `makemigrations --check` clean. No git commands run; worktree left dirty for Forge to commit. |
| 2026-08-25 | US-2 T013-T014 (issue #166) — `_resolve_restriction` gained the concepts axis's term, `Q(slug__in=self.concepts)` joined to the vocabulary term with no subquery, since a plain column filter cannot duplicate a row (T013); a listed concept validates and an unlisted concept of the same vocabulary is refused through `ChipSample`'s real `rock_type` field, the refusal naming the permitted concepts via a new `invalid_restricted_concepts` message rather than reusing `invalid_restricted`'s collection-specific wording (decisions.md D11); the many-valued write guard needed no code change — it already reads `field.get_limit_choices_to()`, proved (and mutation-probed) rather than assumed, on both its forward and reverse branches through `ChipTray`; a slug listed twice offers the concept once, reusing T001's existing collapse. `tests/testapp` gained `ChipSample`/`ChipTray` and one migration (`0006_chip_sample_chip_tray`), `makemigrations --check` clean across all apps. The ModelForm and this package's own widget both offer exactly the listed concepts, by count and membership (T014) — no new code, proved and mutation-probed the same way. Full suite: 1769 passed, 1 skipped (was 1752/1, +17 new). `ruff`/`ruff format`/`mypy`/`deptry` via pre-commit all green; `makemigrations --check` clean. No git commands run; worktree left dirty for Forge to commit. |
| 2026-08-25 | US-2 accepted after review. One correction applied on top: the many-valued write guard refused an unlisted concept correctly but its refusal message still named the vocabulary, not the permitted concepts — inconsistent with both `ConceptField.validate` and the collection axis's own precedent (T008). The guard now names the permitted concepts, with a test that fails against the previous behaviour. Full suite: 1770 passed, 1 skipped. |
| 2026-08-25 | US-3 T015-T018 (issue #167) — `_branch_closure(vocabulary, branch)` added to `fields.py`: iterative widening over stored `BROADER` edges (target→source per round, minus everything already seen), unit-tested directly against a three-level tree, a childless root, a wide sibling level, and a root belonging to another vocabulary (T015); a two-edge cycle built with two ordinary `add_broader()` calls terminates and yields each concept once, bounded by a `SIGALRM` so a regression fails the test rather than hanging the suite (T016); `_resolve_restriction` gained the branch term, `Q(pk__in=_branch_closure(...))` joined to the vocabulary term, mirroring the collection axis's subquery shape (T018 resolution half); `tests/testapp` gained `BranchSample`/`BranchTray` (branch="igneous" within "rock-type") and one consolidated migration (`0007_branch_sample_branch_tray`), `makemigrations --check` clean across all apps; through `BranchSample`'s real `rock_type` field, the root itself validates, a grandchild validates, a sibling branch is refused, and the concept the root sits below is refused — the last two are the ones an inverted walk would pass anyway (T017); the refusal now names the branch root via a new `invalid_restricted_branch` message (`decisions.md` D11's `invalid_restricted_<axis>` pattern), extended on both `ConceptField.validate()` and the many-valued write guard `_refuse_concepts_the_restriction_does_not_admit`, which needed no other change since it already reads `field.get_limit_choices_to()`; the ModelForm and this package's own widget both offer exactly the closure, by count and membership, and a concept added below the root after construction appears on the next read with no restart (T018 choices/live-update half). Every acceptance criterion claimed as covered by existing machinery (the write-guard's reuse, the "no new code" form-choice paths, and the two validation tests that only assert a positive result) was mutation-probed rather than assumed: removing the branch term from `_resolve_restriction` failed 10 dependent tests, and excluding the root itself from the closure's seed set failed the closure's own direct tests plus `test_the_root_itself_validates`; both mutations were reverted after confirming the failure. Full suite: 1796 passed, 1 skipped (was 1770/1, +26 new). `ruff check`/`ruff format --check` on every touched test file green; `makemigrations --check` clean across all apps. No git commands run; worktree left dirty for Forge to commit. |
| 2026-08-25 | US-5 T019-T021 (issue #169) — `check_concept_field_restriction_targets` added to `checks.py`, registered in `apps.ready()` alongside the existing four, emitting a new `controlled_vocabularies.W005` rather than extending W001 (T019). Walks every declared `ConceptField`/`ConceptsField` once and batches the three restriction axes into three queries total (`collection`, `concepts`, `branch`), regardless of field count — asserted directly via `CaptureQueriesContext`. **Resolves on the `(vocabulary slug, target slug)` pair**, never a flat set: each axis's existing set is built with `.values_list("scheme__slug", "slug")` and every declared target is tested as a pair against it, so a collection or concept slug that exists only in a *different* vocabulary is still reported absent from the one the field names — the design's central risk, proved directly with `CoreSample` (collection) and `ChipSample` (concepts) fixtures naming a slug that collides with one in the "Mineral" scheme, and mutation-probed: temporarily flattening `_existing_pairs` to a bare `slug__in`/`slug` lookup (dropping the scheme constraint) made exactly those two tests fail, confirming they are not vacuous; reverted immediately after. `checks.Warning`, never `Error` (T020's `manage.py check`/`makemigrations`/`migrate` sequence against a real, freshly migrated but empty database proves none of the three raise or block, with an in-script assertion that W005 actually fired first — proving the guard isn't silently making the whole scenario vacuous). The three silences (T020): a database with no tables yet reuses `checks.py:65-68`'s own `DatabaseError` guard and reasoning, reproduced rather than duplicated in spirit; a collection that exists and holds no members is not reported (existence, not membership, is what's tested); and a project silencing `controlled_vocabularies.W005` via `SILENCED_SYSTEM_CHECKS` sees nothing. No enforcement code touched — `fields.py`, `forms.py` and `views.py` are unchanged; `tests/testapp/models.py`'s existing `CoreSample`/`DrillCore`/`ChipSample`/`ChipTray`/`BranchSample`/`BranchTray` fixtures (already declared by US-1/US-2/US-3) supplied every restriction shape needed, so no new model or migration was required. Full suite: 1808 passed, 1 skipped (was 1796/1, +12 new). `ruff check`/`ruff format --check` on the touched test file green; `pre-commit run --all-files` green (ruff, mypy, deptry); `makemigrations --check --dry-run` clean across all apps. No git commands run; worktree left dirty for Forge to commit. |
